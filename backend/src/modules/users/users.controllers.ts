import { Request, Response } from "express";

export class UserController {
    constructor() { }

    async create(req: Request, res: Response): Promise<{}> {
        return {}
    }

    async readOne(req: Request, res: Response): Promise<{}> {
        return {}
    }
}